spring:
  application:
    name: Rocketvillain-HealingPets
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/villaindb
    username: villain
    password: villain
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    properties:
      hibernate:
        '[format_sql]': true
logging:
  level:
    root: info